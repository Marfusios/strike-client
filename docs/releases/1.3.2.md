Refreshed dependencies and build tooling while retaining compatibility with existing consumers.

- Added a .NET 10 target alongside .NET Core 3.1, .NET 6, and .NET 8.
- Updated framework-specific dependencies and test tooling to compatible patched releases.
- Added offline HTTP, serialization, error-handling, and safety regression tests.
- Restricted live integration tests to reviewed read-only requests and added an HTTP guard that blocks writes.
- Switched NuGet publication to GitHub Actions trusted publishing with OIDC.

Validation: Release and Debug builds passed; 58 offline tests and 4 read-only live tests passed. Package consumers were checked on .NET Core 3.1 and .NET 5 through 10, and public API compatibility was verified against 1.3.1.

NuGet: [StrikeWallet.Client 1.3.2](https://www.nuget.org/packages/StrikeWallet.Client/1.3.2).
